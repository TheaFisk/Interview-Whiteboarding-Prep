
int main()
{
    //Fibonacci Sequence in C

    int n;
    printf("Nth number");
    scanf("%d", &n);
    
    int loop;
    double prior;
    double current;
    double next;

    while (n > loop)
    {
        printf(next);
    }
    return 0;
}