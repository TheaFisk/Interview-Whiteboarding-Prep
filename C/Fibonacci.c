
#include <stdio.h>

int main()
{
    //Fibonacci Sequence in C

    int n;
    printf("Nth number");
    scanf("%d", n);

    int loop;
    int prior;
    int current;
    int next;

    printf("%d", next);
    /*
        while (n > loop)
    {
        printf("%d", next);
    }
    */

    return 0;
}